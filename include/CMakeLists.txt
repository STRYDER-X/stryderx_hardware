target_include_directories(${CAMERA_SERVER_EXE_NAME} PRIVATE
	"${CMAKE_CURRENT_SOURCE_DIR}"
	${OpenCV_INCLUDE_DIRS}
)

target_include_directories(${JOYSTICK_TELEOP_EXE_NAME} PRIVATE
	"${CMAKE_CURRENT_SOURCE_DIR}"
)
